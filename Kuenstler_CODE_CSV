## Tool zur Gewinnung einer Liste von fehlenden VG-Bild-Einträgen
# Pandas-Import für die bessere Ansicht 
import pandas as pd

# Umbenennung der CSV-Datei
kue_file = "kueShortExport.csv"
for line in open (kue_file):
    print (line.split(";")[2], [8])

# Umbenennung zur besseren Lesbarkeit
Dok_ID = "3000"
VGBild = "3189"
Comment = "99fm"

# loop: Eine Datei fängt an mit Dok_ID und endet mit VGBild. 
for "VGBild"= False:
    print (line)
    ...
# mache eine LIste aus VGBild = "false" 
...
# benenne sie mit "VGBild_ID_Nachtrag"
...

Dok_ID ="*"
Norm_ID ="false" 

# Mail integrierren an Redaktion
import smtplib
smtpObj = smtplib.SMTP("smtp.gmail.com", 587)
smtpObj.ehlo()
smtpObj.starttls()
smtpObj.login("martha.rosenkoetter@gmail.com", "WONTTELLYA")
smtpObj.sendmail("martha.rosenkoetter@gmail.com", "Subject: New VGBild-list to proove")
smtpObj.quit()

## NEXT: Datei (FehlenderVGBild) automatisch ein mit einem timestamp einbetten
